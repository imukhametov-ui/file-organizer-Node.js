# File Organizer CLI

## Commands

npm run scan -- ./test-folder
npm run duplicates -- ./test-folder
npm run organize -- ./test-folder
npm run cleanup -- ./test-folder
# File Organizer CLI

A Node.js CLI tool to scan, organize, find duplicates, and clean up files in a directory.

---

## 📦 Installation

```bash
git clone https://github.com/imukhametov-ui/file-organizer-Node.js.git
cd file-organizer-Node.js/file-organizer
npm install